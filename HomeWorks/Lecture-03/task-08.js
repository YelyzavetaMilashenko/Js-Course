// № 8
const eng = []
const rus = []
const colors = {green: 'зеленый', 
red: 'красный', 
blue: 'голубой', 
white: 'белый', 
yellow: 'желтый'} 
for (const key in colors) {
  if (colors, key) {
    let element = colors[key];
   }
eng.push(key)
rus.push(colors[key])
  // console.log(element)
  // console.log(key)
}
console.log(eng)
console.log(rus)